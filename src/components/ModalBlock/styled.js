import styled from "styled-components";
import ModalFormLogin from "../ModalFormLogin/styled";

const ModalBlock = styled.div`
  position: absolute;
  z-index: 2;
  left: calc(50% - (366px / 2));
  top: calc(50% - (439px / 2));
  opacity: 1;
`;

export default ModalBlock;
